# Congress Twin — example environment
# Copy to .env and set values. With POSTGRES_HOST set, the app uses PostgreSQL
# and auto-seeds simulated congress data for the default plan when the DB is empty.

# --- PostgreSQL (existing project). When set, SQLite is not used.
POSTGRES_HOST=192.168.0.100
POSTGRES_PORT=5432
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DATABASE=tpcds
POSTGRES_SCHEMA=congress_twin

# --- SQLite (used only when POSTGRES_HOST is not set)
# SQLITE_DB_PATH=congress_twin.db

# --- API
# CORS_ORIGINS=http://localhost:3000,http://localhost:3002
# PLANNER_PLAN_URL=

# --- MS Graph (optional; for live Planner sync)
# GRAPH_CLIENT_ID=
# GRAPH_CLIENT_SECRET=
# GRAPH_TENANT_ID=

# --- Chat semantic layer (Hybrid Phase 1). If GROQ_API_KEY or CHAT_LLM_API_KEY is set, intent extraction uses LLM; else regex.
# Groq (recommended): same as sku_validation / fmcg-mroi
# GROQ_API_KEY=gsk_...
# GROQ_MODEL=llama-3.1-8b-instant
# Or OpenAI:
# CHAT_LLM_API_KEY=sk-...
# CHAT_LLM_MODEL=gpt-4o-mini
# CHAT_LLM_BASE_URL=   # Optional for custom endpoint
# CHAT_TRACE_STORE_PATH=   # Optional; default: chat_traces.db next to sqlite DB
