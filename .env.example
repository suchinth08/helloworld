# Congress Twin (Novartis Planner) — copy to .env
# SQLite database (embedded, no server required)
# Default: congress_twin.db in project root. Use absolute path for custom location.
SQLITE_DB_PATH=congress_twin.db

# MS Graph (Phase 1 — optional; when set, sync can use live Planner)
# GRAPH_CLIENT_ID=
# GRAPH_CLIENT_SECRET=
# GRAPH_TENANT_ID=

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_ALL=true
